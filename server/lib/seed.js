/* ============================================================
   Campus Market — Database initialization
   Production system: no demo data seeding
   ============================================================ */
'use strict';

function seedIfEmpty(db) {
  // Production system: no automatic seeding
  // Users must be created through the signup process
}

module.exports = { seedIfEmpty };
